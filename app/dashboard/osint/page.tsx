import { SituationScreen } from "@/components/osint-v2";

export const metadata = {
  title: "OSINT Situational Awareness | Alpha-Quant-Copilot",
};

export default function OSINTPage() {
  return <SituationScreen />;
}
